{
  "name": "pgj-2026-multiplayer-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node.js + WebSocket server for a multiplayer web game",
  "main": "src/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run docs:dev\"",
    "dev:server": "nodemon src/server.js",
    "start": "node src/server.js",
    "lint": "echo \"(no lint configured)\"",
    "docs:dev": "npm --prefix docs run dev -- --host"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.0"
  }
}
